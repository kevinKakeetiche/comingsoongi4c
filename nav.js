(function(){const btn=document.querySelector('.menu-btn'), menu=document.querySelector('.mobile-menu'); if(btn&&menu){btn.addEventListener('click',()=>{menu.classList.toggle('open');btn.setAttribute('aria-expanded',menu.classList.contains('open'));});menu.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>menu.classList.remove('open')))} document.querySelectorAll('[data-demo-form]').forEach(f=>f.addEventListener('submit',e=>{e.preventDefault();const msg=f.querySelector('[data-form-message]');if(msg)msg.textContent='Thank you. This demo form is ready to be connected to the GI4C backend.';})); const chat=document.querySelector('#ai-chat'); const input=document.querySelector('#ai-input'); const form=document.querySelector('#ai-form'); if(form&&chat&&input){form.addEventListener('submit',e=>{e.preventDefault();const v=input.value.trim();if(!v)return;chat.insertAdjacentHTML('beforeend',`<div class="bubble user"></div>`);chat.lastElementChild.textContent=v;chat.insertAdjacentHTML('beforeend',`<div class="bubble bot">Thanks. The GI4C AI Assistant interface is active. The production AI model and knowledge base can be connected here next.</div>`);input.value='';chat.scrollTop=chat.scrollHeight;});}})();
